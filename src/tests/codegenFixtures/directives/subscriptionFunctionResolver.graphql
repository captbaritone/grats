directive @asyncIterable on FIELD_DEFINITION

directive @exported(
  jsModulePath: String!
  tsModulePath: String!
  functionName: String!
  argCount: Int!
) on FIELD_DEFINITION

type Subscription {
  posts: String
    @asyncIterable
    @exported(
      jsModulePath: "../foo.js"
      tsModulePath: "../foo.ts"
      functionName: "salutation"
      argCount: 2
    )
}
