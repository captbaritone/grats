-----------------
INPUT
----------------- 
/** @GQLType */
class Query {
  /** @GQLField */
  hello: string;
}

/** @GQLEnum */
enum Enum {
  VALID = "VALID",
  INVALID = "INVALID",
}

-----------------
OUTPUT
-----------------
directive @renameField(name: String!) on FIELD_DEFINITION

type Query {
  hello: String
}

enum Enum {
  VALID
  INVALID
}