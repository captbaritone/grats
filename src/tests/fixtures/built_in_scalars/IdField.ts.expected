-----------------
INPUT
----------------- 
import { ID } from "../../../Types";

/** @gqlType */
export default class SomeType {
  /** @gqlField */
  id(): ID {
    return "QUERY_ID";
  }
}

-----------------
OUTPUT
-----------------
-- SDL --
type SomeType @exported(exportName: "SomeType", tsModulePath: "grats/src/tests/fixtures/built_in_scalars/IdField.ts") {
  id: ID @metadata(argCount: 0)
}
-- TypeScript --
import { SomeType as SomeTypeClass } from "./IdField";
import { GraphQLSchema, GraphQLObjectType, GraphQLID } from "graphql";
export function getSchema(): GraphQLSchema {
    const SomeTypeType: GraphQLObjectType = new GraphQLObjectType({
        name: "SomeType",
        fields() {
            return {
                id: {
                    name: "id",
                    type: GraphQLID
                }
            };
        }
    });
    return new GraphQLSchema({
        types: [SomeTypeType]
    });
}
const typeNameMap = new Map();
typeNameMap.set(SomeTypeClass, "SomeType");

function resolveType(obj: any): string {
  if (typeof obj.__typename === "string") {
    return obj.__typename;
  }

  let prototype = Object.getPrototypeOf(obj);
  while (prototype) {
    const name = typeNameMap.get(prototype.constructor);
    if (name != null) {
      return name;
    }
    prototype = Object.getPrototypeOf(prototype);
  }

  throw new Error("Cannot find type name");
}