-----------------
INPUT
----------------- 
/** @gqlType */
export class User<T> implements Friendly {
  __typename: "User";
  /** @gqlField */
  name: string;

  /** @gqlField */
  friend: T;
}

/** @gqlInterface */
interface Friendly {
  /** @gqlField */
  name: string;
}

/** @gqlType */
class Dog {
  /** @gqlField */
  name: string;
  /** @gqlField */
  bestFriend: User<Dog>;
}

-----------------
OUTPUT
-----------------
src/tests/fixtures/generics/todo/genericTypeImplementsInterface.ts:2:33 - error: Unexpected interface implementation on generic `@gqlType`. Generic GraphQL types will be synthesize into types with different names, but types which implement an interface must define an explicit `__typename` property matching their GraphQL name.

2 export class User<T> implements Friendly {
                                  ~~~~~~~~
