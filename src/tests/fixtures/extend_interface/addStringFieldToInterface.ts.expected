-----------------
INPUT
----------------- 
/** @gqlInterface */
interface IPerson {
  name: string;
  /** @gqlField */
  hello: string;
}

/** @gqlField */
export function greeting(person: IPerson): string {
  return `Hello ${person.name}!`;
}

/** @gqlType */
class User implements IPerson {
  __typename: "User";
  name: string;
  /** @gqlField */
  hello: string;
}

/** @gqlType */
class Admin implements IPerson {
  __typename: "Admin";
  name: string;
  /** @gqlField */
  hello: string;
}

-----------------
OUTPUT
-----------------
src/tests/fixtures/extend_interface/addStringFieldToInterface.ts:2:1 - error: Cannot extend non-object type "IPerson".

  2 interface IPerson {
    ~~~~~~~~~~~~~~~~~~~
  3   name: string;
    ~~~~~~~~~~~~~~~
... 
  5   hello: string;
    ~~~~~~~~~~~~~~~~
  6 }
    ~

  src/tests/fixtures/extend_interface/addStringFieldToInterface.ts:9:1
     9 export function greeting(person: IPerson): string {
       ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    10   return `Hello ${person.name}!`;
       ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    11 }
       ~
    Related location
