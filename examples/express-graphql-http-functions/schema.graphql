# Schema generated by Grats (https://grats.capt.dev)
# Do not manually edit. Regenerate by running `npx grats`.
interface IPerson {
  name: String!
}

input UserDataInput {
  groupIDs: [String!]
  name: String!
}

type Group {
  id: String!
  members: [User!]!
  name: String!
}

type Mutation {
  addUser(userData: UserDataInput!): User!
  removeUser(id: String!): String!
  updateUser(id: String!, userData: UserDataInput!): User!
}

type Query {
  allUsers: [User!]!
  me: User!
  person: IPerson!
  userById(id: String!): User!
}

type User implements IPerson {
  groups: [Group!]!
  id: String!
  name: String!
}
